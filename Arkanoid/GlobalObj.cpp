#include "GlobalObj.h"

const float GlobalObj::windowWidth = 800.f;
const float GlobalObj::barWidth = 300.f;
const float GlobalObj::windowHeight = 600.f;
//sf::RenderWindow GlobalObj::mainWindow(sf::VideoMode(windowWidth + barWidth, windowHeight), "Arkanoid", sf::Style::Close | sf::Style::Titlebar);
//sf::RenderWindow GlobalObj::menuWindow(sf::VideoMode(windowWidth, windowHeight), "Arkanoid", sf::Style::Close | sf::Style::Titlebar);